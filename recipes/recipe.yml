name: cloche-enterprise
description: ---
base-image: quay.io/almalinuxorg/almalinux-bootc
image-version: latest

modules:
  - type: containerfile
    snippets:
      - RUN dnf install -y epel-release && dnf config-manager --set-enabled crb

      # 
      - RUN echo "9" > /etc/dnf/vars/releasever
      
      # 
      - RUN sed -i 's/$releasever/9/g' /etc/yum.repos.d/*.repo
      
      # 
      - RUN dnf install -y epel-release && dnf config-manager --set-enabled crb
      
      # 
      - RUN sed -i 's/$releasever/9/g' /etc/yum.repos.d/epel*.repo
#  - type: dnf
#    install:
#      packages:
        #- "@Workstation"
        #- gdm

  #- type: systemd
    #system:
      #enabled:
        #- gdm.service
        #- NetworkManager.service
